{
  "name": "tupatrimonio-monorepo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run build:location && npm run dev --workspace=apps/web",
    "dev:marketing": "npm run build:location && npm run dev --workspace=apps/marketing",
    "build": "npm run build:location && npm run build --workspaces --if-present",
    "build:web": "npm run build:location && npm run build --workspace=apps/web",
    "build:web-standalone": "npm run build --workspace=apps/web",
    "build:marketing": "npm run build:location && npm run build --workspace=apps/marketing",
    "build:location": "npm run build --workspace=packages/location",
    "start": "npm run start --workspace=apps/web",
    "start:marketing": "npm run start --workspace=apps/marketing",
    "lint": "npm run lint --workspaces"
  },
  "devDependencies": {
    "@eslint/eslintrc": "^3",
    "eslint": "^9",
    "supabase": "^2.53.6",
    "typescript": "^5"
  }
}
